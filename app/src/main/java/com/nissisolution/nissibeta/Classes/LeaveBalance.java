package com.nissisolution.nissibeta.Classes;

public class LeaveBalance {
    public String slug, total, remain;

    public LeaveBalance(String slug, String total, String remain) {
        this.slug = slug;
        this.total = total;
        this.remain = remain;
    }
}
